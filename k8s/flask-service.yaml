apiVersion: v1
kind: Service
metadata:
  name: flask-app
  namespace: fluidai-demo
  labels:
    app: flask-app
spec:
  type: ClusterIP
  selector:
    app: flask-app      
  ports:
  - name: http
    port: 80              
    targetPort: 5000      
    protocol: TCP
